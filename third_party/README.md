# Third-party dependencies

Keep external libraries isolated here or declared explicitly by the build system. Do not include third-party headers in `src/core/` unless the dependency is confirmed portable to the STM32 target.
